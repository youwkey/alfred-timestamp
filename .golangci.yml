linters:
  enable-all: true
  disable:
    - scopelint # deprecated
    - interfacer # deprecated
    - maligned # deprecated
    - golint # deprecated
linters-settings:
  varnamelen:
    min-name-length: 1
  gomnd:
    ignored-functions:
      - 'strconv.*'
issues:
  exclude-use-default: false
